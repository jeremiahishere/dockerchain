#!/usr/bin/env ruby

begin
  require 'dockerchain'
rescue LoadError
  require 'rubygems'
  require 'dockerchain'
end

if ARGV.empty? || !ARGV[0]
  raise 'Command syntax: dockerchain path/to/dockerchain.yml'
end

Dockerchain::Runner.run(ARGV[0])
