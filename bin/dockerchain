#!/usr/bin/env ruby

begin
  require 'dockerchain'
rescue LoadError
  require 'rubygems'
  require 'dockerchain'
end

if ARGV.empty? || !ARGV[0]
  puts 'Syntax: dockerchain path/to/dockerchain.yml'
  Kernel.exit(1)
end

runner = Dockerchain::Runner.new
runner.run(ARGV[0])
